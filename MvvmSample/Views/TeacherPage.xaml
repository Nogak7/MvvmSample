<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MvvmSample.Views.TeacherPage"
             xmlns:models="clr-namespace:BindingToObject.Models"
            xmlns:viewModels="clr-namespace:MvvmSample.ViewModels"
             x:DataType="viewModels:TeacherPageViewModel"
             Title="TeacherPage">
    <VerticalStackLayout>
        <Label Text="{Binding Text}">
            
        </Label>
        <Button Text="Enter Name" Command="{Binding ChangeText}">
            
        </Button>
        
        
        
    </VerticalStackLayout>
</ContentPage>